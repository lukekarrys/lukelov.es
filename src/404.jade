h1 This page does not exists. Here are the blog posts though:
ul.posts
  each article, slug in public.articles._data
    li
      span.date #{article.date}
      |  » 
      a(href="/#{slug}") #{article.title}
